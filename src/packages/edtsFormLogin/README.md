# EdtsFormLogin

Login-style form wrapper for Mendix pluggable widgets.

Use the `Content` slot to drag input widgets into the form. Configure `On Submit`, `On Back`, and optional secondary action from Mendix Studio Pro.
